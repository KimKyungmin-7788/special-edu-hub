/**
 * 랜딩 '글쓰기' 진입 카드 타일 노출 토글.
 *
 * 최신 수업자료 그리드 첫 칸의 '+ 글쓰기' 타일을 켜고 끈다.
 * 빼려면 false 로 끄거나, 이 파일 + RegisterCtaCard.tsx + Home.tsx 의
 * 가드 블록을 지우면 깔끔히 제거된다.
 *
 * (목록 페이지 하단의 '글쓰기' 버튼은 별개 — 상시 노출.)
 */
export const registerCta = {
  showCardTile: true,
}
